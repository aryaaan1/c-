#include<iostream>
using namespace std;
int main(){
    int x;
    cin>>x;
    // (condition) ? if true then print : if false then print ;
    (x%2==0) ? cout<<"even" : cout<<"odd" ;
}
    // used in case of if else