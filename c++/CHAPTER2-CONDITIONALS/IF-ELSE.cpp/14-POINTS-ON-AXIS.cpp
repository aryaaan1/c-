#include<iostream>
using namespace std;
int main(){
    int x,y;
    cout<<"enter x :";
    cin>>x;
    cout<<"enter  y :";
    cin>>y;
    

    if(x==0 and y>0)
    cout<<"point lies on positive y axis";
    if(x==0 and y<0)
    cout<<"point lies on negative y axis";
if(x>0 and y==0)
    cout<<"point lies on positive x axis";
if(x<0 and y==0)
    cout<<"point lies on negative x axis";
     if(x>0 and y>0)
    cout<<"point lies on 1ST QUADRANT";
    if(x>0 and y<0)
    cout<<"point lies on 4TH QUADRANT";
if(x<0 and y>0)
    cout<<"point lies on 3RD QUADRANT";
if(x<0 and y<0)
    cout<<"point  lies on 4th QUADRANT  ";














}
   